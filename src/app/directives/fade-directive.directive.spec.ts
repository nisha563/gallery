import { FadeDirectiveDirective } from './fade-directive.directive';

describe('FadeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
